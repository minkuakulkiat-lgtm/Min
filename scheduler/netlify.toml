[build]
  command = "npm run build"
  publish = "dist"

# ให้ทุก path เปิดหน้าเว็บเดียวกันได้ (single page app)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"
